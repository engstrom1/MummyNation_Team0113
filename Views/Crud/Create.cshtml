@{

}

<form method="post" asp-controller="Crud" asp-action="CreateConfirmed">

    <div asp-validation-summary="All" class="text-danger"></div>

    <h1>Add New Burial:</h1>

    @foreach (var property in typeof(Burialmain).GetProperties())
    {
            @if (@property.Name != "Id")
            {
                <div class="form-group">
                    <label asp-for="@property.Name">@property.Name:</label>
                    <input asp-for="@property.Name" name ="@property.Name" value="" type ="@property.PropertyType.Name"class="form-control" />
                </div>
            }
    }
    <button type="submit" class="btn btn-success btn-sm float-end">Create entry</button>
</form>

<br />
<br />
